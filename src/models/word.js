// Editable properties, stored in state of the forms
export const WORD = {
  text: '',
  image: '',
  notes: '',
  gender: ''
};

export const WORD_PROPS = Object.keys(WORD);

export const WORD_TEXT_MAX_LENGTH = 40;

export const WORD_GENDERS = ['f', 'm', ''];
